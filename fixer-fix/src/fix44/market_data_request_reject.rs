// Code generated by fixer-gen. DO NOT EDIT.
#![allow(clippy::new_without_default)]
#![allow(clippy::needless_pass_by_value)]
#![allow(clippy::too_many_arguments)]
#![allow(unused_imports)]

use fixer::message::Message;
use fixer::fix_string::FIXString;
use fixer::errors::MessageRejectErrorEnum;
use fixer::session::session_id::SessionID;
use fixer::repeating_group::{Group, GroupTemplate, RepeatingGroup, group_element};
use std::borrow::{Borrow, BorrowMut};


use crate::field;
use crate::tag;

/// `MarketDataRequestReject` is the `fix44` `MarketDataRequestReject` type, `MsgType` = Y.
pub struct MarketDataRequestReject {
    pub message: Message,
}

impl MarketDataRequestReject {
    /// Creates a new `MarketDataRequestReject` with required fields.
    pub fn new(md_req_id: field::MDReqIDField) -> Self {
        let mut msg = Message::new();
        msg.header.set_field(tag::MSG_TYPE, FIXString::from("Y".to_string()));

        msg.body.set_field(tag::MD_REQ_ID, md_req_id.0);

        Self { message: msg }
    }

    /// Creates a `MarketDataRequestReject` from an existing `Message`.
    pub fn from_message(msg: Message) -> Self {
        Self { message: msg }
    }

    /// Returns the underlying `Message`.
    pub fn to_message(self) -> Message {
        self.message
    }




    /// Sets `EncodedText`, Tag 355.
    pub fn set_encoded_text(&mut self, v: String) {
        self.message.body.set_field(tag::ENCODED_TEXT, FIXString::from(v));
    }

    /// Gets `EncodedText`, Tag 355.
    pub fn get_encoded_text(&self) -> Result<String, MessageRejectErrorEnum> {
        let mut fld = field::EncodedTextField::new(String::new());
        self.message.body.get_field(tag::ENCODED_TEXT, &mut fld.0)?;
        Ok(fld.value().to_string())
    }


    /// Returns true if `EncodedText` is present, Tag 355.
    pub fn has_encoded_text(&self) -> bool {
        self.message.body.has(tag::ENCODED_TEXT)
    }




    /// Sets `EncodedTextLen`, Tag 354.
    pub fn set_encoded_text_len(&mut self, v: isize) {
        self.message.body.set_field(tag::ENCODED_TEXT_LEN, fixer::fix_int::FIXInt::from(v));
    }

    /// Gets `EncodedTextLen`, Tag 354.
    pub fn get_encoded_text_len(&self) -> Result<isize, MessageRejectErrorEnum> {
        let mut fld = field::EncodedTextLenField::new(0);
        self.message.body.get_field(tag::ENCODED_TEXT_LEN, &mut fld.0)?;
        Ok(fld.value())
    }


    /// Returns true if `EncodedTextLen` is present, Tag 354.
    pub fn has_encoded_text_len(&self) -> bool {
        self.message.body.has(tag::ENCODED_TEXT_LEN)
    }




    /// Sets `MDReqID`, Tag 262.
    pub fn set_md_req_id(&mut self, v: String) {
        self.message.body.set_field(tag::MD_REQ_ID, FIXString::from(v));
    }

    /// Gets `MDReqID`, Tag 262.
    pub fn get_md_req_id(&self) -> Result<String, MessageRejectErrorEnum> {
        let mut fld = field::MDReqIDField::new(String::new());
        self.message.body.get_field(tag::MD_REQ_ID, &mut fld.0)?;
        Ok(fld.value().to_string())
    }


    /// Returns true if `MDReqID` is present, Tag 262.
    pub fn has_md_req_id(&self) -> bool {
        self.message.body.has(tag::MD_REQ_ID)
    }




    /// Sets `MDReqRejReason`, Tag 281.
    pub fn set_md_req_rej_reason(&mut self, v: String) {
        self.message.body.set_field(tag::MD_REQ_REJ_REASON, FIXString::from(v));
    }

    /// Gets `MDReqRejReason`, Tag 281.
    pub fn get_md_req_rej_reason(&self) -> Result<String, MessageRejectErrorEnum> {
        let mut fld = field::MDReqRejReasonField::new(String::new());
        self.message.body.get_field(tag::MD_REQ_REJ_REASON, &mut fld.0)?;
        Ok(fld.value().to_string())
    }


    /// Returns true if `MDReqRejReason` is present, Tag 281.
    pub fn has_md_req_rej_reason(&self) -> bool {
        self.message.body.has(tag::MD_REQ_REJ_REASON)
    }




    /// Sets `NoAltMDSource`, Tag 816.
    pub fn set_no_alt_md_source(&mut self, f: NoAltMDSourceRepeatingGroup) {
        self.message.body.set_group(f.0);
    }

    /// Gets `NoAltMDSource`, Tag 816.
    pub fn get_no_alt_md_source(&self) -> Result<NoAltMDSourceRepeatingGroup, MessageRejectErrorEnum> {
        let g = NoAltMDSourceRepeatingGroup::new();
        Ok(NoAltMDSourceRepeatingGroup(self.message.body.get_group(g.0)?))
    }


    /// Returns true if `NoAltMDSource` is present, Tag 816.
    pub fn has_no_alt_md_source(&self) -> bool {
        self.message.body.has(tag::NO_ALT_MD_SOURCE)
    }




    /// Sets `Text`, Tag 58.
    pub fn set_text(&mut self, v: String) {
        self.message.body.set_field(tag::TEXT, FIXString::from(v));
    }

    /// Gets `Text`, Tag 58.
    pub fn get_text(&self) -> Result<String, MessageRejectErrorEnum> {
        let mut fld = field::TextField::new(String::new());
        self.message.body.get_field(tag::TEXT, &mut fld.0)?;
        Ok(fld.value().to_string())
    }


    /// Returns true if `Text` is present, Tag 58.
    pub fn has_text(&self) -> bool {
        self.message.body.has(tag::TEXT)
    }


}

/// `RouteOut` is the callback type for routing `MarketDataRequestReject` messages.
pub type RouteOut = fn(msg: MarketDataRequestReject, session_id: SessionID) -> Result<(), MessageRejectErrorEnum>;

/// Route type returned by the `route` function.
pub type Route = (&'static str, &'static str, Box<dyn Fn(&Message, SessionID) -> Result<(), MessageRejectErrorEnum> + Send>);

/// Returns the begin string, message type, and route function for `MarketDataRequestReject`.
pub fn route(router: RouteOut) -> Route {
    let r = move |msg: &Message, session_id: SessionID| -> Result<(), MessageRejectErrorEnum> {
        router(MarketDataRequestReject::from_message(msg.clone()), session_id)
    };
    ("FIX.4.4", "Y", Box::new(r))
}


/// `NoAltMDSource` is an entry in the `NoAltMDSource` repeating group, Tag 816.
pub struct NoAltMDSource<G>(pub G);

impl<G: Borrow<Group>> NoAltMDSource<G> {
    fn group(&self) -> &Group {
        self.0.borrow()
    }



    /// Gets `AltMDSourceID`, Tag 817.
    pub fn get_alt_md_source_id(&self) -> Result<String, MessageRejectErrorEnum> {
        let mut fld = field::AltMDSourceIDField::new(String::new());
        self.group().field_map.get_field(tag::ALT_MD_SOURCE_ID, &mut fld.0)?;
        Ok(fld.value().to_string())
    }


    /// Returns true if `AltMDSourceID` is present, Tag 817.
    pub fn has_alt_md_source_id(&self) -> bool {
        self.group().field_map.has(tag::ALT_MD_SOURCE_ID)
    }


}

impl<G: BorrowMut<Group>> NoAltMDSource<G> {
    fn group_mut(&mut self) -> &mut Group {
        self.0.borrow_mut()
    }



    /// Sets `AltMDSourceID`, Tag 817.
    pub fn set_alt_md_source_id(&mut self, v: String) {
        self.group_mut().field_map.set_field(tag::ALT_MD_SOURCE_ID, FIXString::from(v));
    }



}

/// `NoAltMDSourceRepeatingGroup` is the `NoAltMDSource` repeating group, Tag 816.
pub struct NoAltMDSourceRepeatingGroup(pub RepeatingGroup);

impl NoAltMDSourceRepeatingGroup {
    /// Creates an empty `NoAltMDSource` group with the template from the FIX spec.
    pub fn new() -> Self {
        let template: GroupTemplate = vec![


            group_element(tag::ALT_MD_SOURCE_ID),


        ];
        Self(RepeatingGroup::new(tag::NO_ALT_MD_SOURCE, template))
    }

    /// Appends an entry and returns it for population.
    pub fn add(&mut self) -> NoAltMDSource<&mut Group> {
        NoAltMDSource(self.0.add())
    }

    /// Returns the `i`th entry.
    pub fn get(&self, i: usize) -> NoAltMDSource<&Group> {
        NoAltMDSource(self.0.get(i))
    }

    /// Returns the number of entries.
    pub fn len(&self) -> usize {
        self.0.len()
    }

    /// Returns true if the group has no entries.
    pub fn is_empty(&self) -> bool {
        self.0.is_empty()
    }
}

impl Default for NoAltMDSourceRepeatingGroup {
    fn default() -> Self {
        Self::new()
    }
}

